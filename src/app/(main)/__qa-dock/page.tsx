// QA interno — NOTA: los folders con prefijo "_" son privadas en el
// App Router de Next (excluidas del routing), así que este archivo NO
// genera ruta. La página real de auditoría vive en /qa-dock
// (src/app/(main)/qa-dock/page.tsx). Este stub solo existe porque el
// entorno de trabajo no permite borrar archivos del mount.
export { default } from '../qa-dock/page';
